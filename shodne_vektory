import numpy as np

a = [3, 1, 2, 4, 5, 4, 4, 3, 2]
prevod_a = np.array(a)
b = [3, 2, 3, 4, 2, 4, 4, 1, 1]
prevod_b = np.array(b)

vypocet_indexu = np.where(prevod_a == prevod_b)[0]

print(f"Místo stejných hodnot: {vypocet_indexu}")
